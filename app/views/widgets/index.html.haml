%h1 Listing widgets

%table.table
  %tr
    %th Name
    %th Graph
    %th Height
    %th Width
    %th{colspan:"3"}

  - @widgets.each do |widget|
    %tr
      %td= widget.name
      %td=# link_to widget.graph.type, graph_path(widget.graph)
      %td= widget.height
      %td= widget.width
      %td= link_to 'Show', widget
      %td= link_to 'Edit', edit_widget_path(widget)
      %td= link_to 'Destroy', widget, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Widget', new_widget_path(Widget.new)
