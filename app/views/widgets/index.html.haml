%h1 Listing widgets

%table
  %tr
    %th Name
    %th Data
    %th
    %th
    %th

  - @widgets.each do |widget|
    %tr
      %td= widget.name
      %td= widget.data
      %td= link_to 'Show', widget
      %td= link_to 'Edit', edit_widget_path(widget)
      %td= link_to 'Destroy', widget, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Widget', new_widget_path
