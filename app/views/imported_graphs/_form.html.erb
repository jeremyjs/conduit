<%= form_for(@imported_graph) do |f| %>
  <% if @imported_graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@imported_graph.errors.count, "error") %> prohibited this imported_graph from being saved:</h2>

      <ul>
      <% @imported_graph.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
