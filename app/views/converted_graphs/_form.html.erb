<%= form_for(@converted_graph) do |f| %>
  <% if @converted_graph.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@converted_graph.errors.count, "error") %> prohibited this converted_graph from being saved:</h2>

      <ul>
      <% @converted_graph.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
