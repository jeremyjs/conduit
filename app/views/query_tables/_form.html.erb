<%= form_for(@query_table) do |f| %>
  <% if @query_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@query_table.errors.count, "error") %> prohibited this query_table from being saved:</h2>

      <ul>
      <% @query_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query_id %><br>
    <%= f.number_field :query_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
